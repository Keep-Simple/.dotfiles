#!/usr/bin/env bash

USER_APPS="";

ROOT_APPS="
nvim,zsh,
shell,lf,
bin,lvim,
zathura
"

SCRIPT_PATH=$(dirname $(realpath $0))

if [[ $1 != 'd' ]];
then 
	ROOT_APPS=$ROOT_APPS USER_APPS=$USER_APPS $SCRIPT_PATH/install
else
	ROOT_APPS=$ROOT_APPS USER_APPS=$USER_APPS $SCRIPT_PATH/uninstall
fi


