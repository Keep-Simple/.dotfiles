[defaults]
# Vendored modules (the stow module). Implicit for ansible, but ansible-lint
# needs it spelled out to resolve `stow:` tasks.
library = library
retry_files_enabled = false
callbacks_enabled = ansible.posix.profile_tasks
stdout_callback = default
result_format = yaml
interpreter_python=auto_silent
host_key_checking = False
